{% for peer in peers -%}{% if peer.state == "absent" -%}
delete protocols bgp {{ localAS }} neighbor {{ peer.ip }} remote-as {{ peer.remote_as }}{% else -%}
set protocols bgp {{ localAS }} neighbor {{ peer.ip }} remote-as {{ peer.remote_as }}
{% endif -%}{% endfor -%}